# 가이드 파일 작성/수정 지침

## 개요

이 문서는 `guides/` 폴더의 GA4 이벤트 가이드 파일을 작성하거나 수정할 때 따라야 할 지침을 설명합니다.

---

## 핵심 원칙

### 1. 하드코딩 금지

가이드 파일에 **사이트별로 다른 값을 하드코딩하지 않습니다**.

**잘못된 예:**
```javascript
dataLayer.push({
  event: "login",  // 하드코딩 - 사이트마다 다름
  method: "email"
});
```

**올바른 예:**
```javascript
// GA4 이벤트: login
// dataLayer event명은 사이트별 설정 참조
dataLayer.push({
  event: "[eventMapping.login]",  // 예: "login_complete"
  method: "email"
});
```

### 2. site-config.json 참조

사이트별로 다른 모든 값은 `sites/{site-id}/site-config.json`을 참조하도록 안내합니다.

```markdown
**참고**: 실제 `event` 값은 사이트마다 다릅니다.
`sites/{site}/site-config.json`의 `eventMapping.login` 값을 확인하세요.
```

### 3. GA4 표준 이벤트 정의 중심

가이드는 GA4 표준 이벤트의 **정의와 용도**에 집중합니다.
구현 세부사항은 사이트 설정 파일로 분리합니다.

---

## 가이드 파일 구조

### 필수 섹션

모든 가이드 파일은 다음 섹션을 포함해야 합니다:

```markdown
# {이벤트명} 이벤트 가이드

## 이벤트 정의
## 1. 시각적 판단 기준 (Primary - 화면에서 찾기)
## 2. GTM 트리거 조건 (검증용)
## 3. 분석 판단 프로세스
## 4. 구현 검증 포인트
## 5. 수집 파라미터
## 6. 관련 이벤트
## 7. 시나리오 예시
## 8. 시나리오 작성 지침
```

---

## 트리거 타입별 작성 방법

### CUSTOM_EVENT (dataLayer.push) 방식

대부분의 GA4 이벤트가 이 방식을 사용합니다.

**섹션 2 작성 예시:**

```markdown
## 2. GTM 트리거 조건 (검증용)

### 현재 구현된 조건
\`\`\`
트리거 타입: CUSTOM_EVENT (dataLayer push)
Custom Event: [사이트별 상이 - site-config.json 참조]
발생 시점: [이벤트 발생 시점]
\`\`\`

### 사이트별 dataLayer event명 예시
| 사이트 | dataLayer event | GA4 이벤트 | 설정 파일 |
|-------|-----------------|-----------|----------|
| Amoremall | `[실제값]` | `{GA4이벤트}` | `sites/amoremall-kr/site-config.json` |
| Innisfree Shopify | `[실제값]` | `{GA4이벤트}` | `sites/innisfree-shopify/site-config.json` |

**참고**: 실제 `event` 값은 사이트마다 다릅니다.
`sites/{site}/site-config.json`의 `eventMapping.{이벤트}` 값을 확인하세요.

### dataLayer 구조

\`\`\`javascript
// GA4 이벤트: {이벤트명}
// dataLayer event명은 사이트별 설정 참조
dataLayer.push({
  event: "[eventMapping.{이벤트}]",  // 예: "{실제값}"
  ecommerce: {
    // 파라미터들
  }
});
\`\`\`
```

### LINK_CLICK (DOM 요소 클릭) 방식

`select_promotion`, `select_item`, `ap_click` 등이 이 방식을 사용합니다.

**섹션 2 작성 예시:**

```markdown
## 2. GTM 트리거 조건 (검증용)

### 현재 구현된 조건
\`\`\`
트리거 타입: LINK_CLICK (링크 클릭)
CSS Selector: [사이트별 상이 - site-config.json 참조]
이벤트 발생: 링크 클릭 시 GTM이 자동으로 GA4 이벤트 전송
\`\`\`

**중요**: dataLayer.push로 이벤트를 발생시키는 것이 아니라,
GTM의 LINK_CLICK 트리거가 요소 클릭을 감지하여 이벤트를 발생시킵니다.

### 사이트별 CSS Selector 예시
| 사이트 | CSS Selector | DOM 속성 | 설정 파일 |
|-------|--------------|----------|----------|
| Amoremall | `a[ap-click-area]` | `ap-click-data` | `sites/amoremall-kr/site-config.json` |
| Innisfree Shopify | `a[data-{속성}]` | `data-{속성}` | `sites/innisfree-shopify/site-config.json` |

**참고**: 실제 CSS Selector와 DOM 속성은 사이트마다 다릅니다.
`sites/{site}/site-config.json`의 `domAttributes.{이벤트}` 값을 확인하세요.

### DOM 속성 구조
\`\`\`html
<!-- Amoremall 방식 -->
<a href="[URL]" ap-click-area="{타입}" ap-click-data='{"key":"value"}'>
  ...
</a>

<!-- Shopify 방식 -->
<a href="[URL]" data-{속성}='{"key":"value"}'>
  ...
</a>
\`\`\`
```

### 기타 트리거 타입

| 트리거 타입 | 사용 이벤트 | 특징 |
|-----------|-----------|------|
| PAGEVIEW | page_view | GTM 내장 트리거, 모든 페이지 |
| HISTORY_CHANGE | page_view (SPA) | SPA 라우트 변경 감지 |
| SCROLL_DEPTH | scroll | 스크롤 깊이 임계값 |
| DOM_READY | view_promotion | DOM 로드 완료 시 |

이 타입들은 사이트별 차이가 적어 site-config 참조가 필수는 아닙니다.

---

## 시나리오 작성 지침 섹션 (섹션 8)

### CUSTOM_EVENT 방식 작성 예시

```markdown
## 8. 시나리오 작성 지침

### 트리거 타입: CUSTOM_EVENT (dataLayer.push)

이 이벤트는 **dataLayer.push로 발생**합니다.
[발생 시점] 프론트엔드에서 dataLayer.push를 호출하여 이벤트를 발생시킵니다.

### 시나리오에 포함할 내용
\`\`\`
O 포함해야 함:
- 예상 dataLayer 구조 (dataLayer.push 코드)
- event 이름 (site-config 참조)
- ecommerce 객체 구조
- items 배열 내 파라미터

X 포함하지 않음:
- GTM LINK_CLICK 트리거 관련 내용
- DOM 속성에서 데이터 추출하는 방식
\`\`\`

### 검증 포인트
\`\`\`
1. dataLayer 확인:
   - 개발자도구 Console에서 dataLayer 배열 확인
   - {이벤트} 이벤트 객체가 push되는지 확인

2. 발생 시점 확인:
   - [구체적인 발생 시점]
\`\`\`
```

### LINK_CLICK 방식 작성 예시

```markdown
## 8. 시나리오 작성 지침

### 트리거 타입: LINK_CLICK (DOM 요소 클릭 감지)

이 이벤트는 **dataLayer.push로 발생하지 않습니다**.
GTM의 LINK_CLICK 트리거가 `[CSS Selector]` 요소 클릭을 감지하여 이벤트를 발생시킵니다.

### 시나리오에 포함할 내용
\`\`\`
O 포함해야 함:
- GTM 트리거 조건 (CSS Selector)
- DOM 속성 구조
- GTM 변수명과 역할
- 변수가 반환하는 데이터 구조
- 파라미터 매핑

X 포함하지 않음:
- "예상 dataLayer 구조" 섹션
- dataLayer.push({ event: "...", ... }) 형태의 코드
\`\`\`

### 검증 포인트
\`\`\`
1. DOM 요소 확인:
   - <a> 태그에 필수 속성 존재 여부
   - 속성값의 JSON 유효성

2. GTM 설정 확인:
   - LINK_CLICK 트리거가 CSS Selector와 매칭되는지
   - JS 변수가 속성값을 올바르게 파싱하는지
\`\`\`
```

---

## 새 사이트 추가 시 가이드 업데이트

새로운 사이트가 추가되면 다음 작업이 필요합니다:

### 1. site-config.json 생성
```
sites/{new-site-id}/site-config.json
```

### 2. 가이드 파일 테이블 업데이트

모든 관련 가이드 파일의 "사이트별 예시" 테이블에 새 사이트 추가:

```markdown
| 사이트 | dataLayer event | GA4 이벤트 | 설정 파일 |
|-------|-----------------|-----------|----------|
| Amoremall | `login_complete` | `login` | `sites/amoremall-kr/site-config.json` |
| Innisfree Shopify | `login_complete` | `login` | `sites/innisfree-shopify/site-config.json` |
| **{새 사이트}** | `{값}` | `login` | `sites/{new-site-id}/site-config.json` |
```

---

## 이벤트 검증 시나리오 작성

### 기존 구조의 한계

```markdown
### Should Fire
- ...

### Should NOT Fire
- ...
```

이 구조는 **단순 발생/미발생만 다루며**, 실제 QA에서 발생하는 복잡한 시나리오를 놓칩니다.

### 개선된 시나리오 구조

```markdown
### 정상 발생 케이스 (Expected Fire) ✅
- 상황: [구체적 상황]
- 예상 이벤트: [이벤트명]
- 예상 파라미터: [주요 파라미터]

### 정상 미발생 케이스 (Expected No Fire) ✅
- 상황: [구체적 상황]
- 이유: [미발생 사유]

### 오발생 위험 케이스 (Wrong Fire Risk) ⚠️
- 상황: [이벤트 버블링, 잘못된 셀렉터 등]
- 확인 방법: [검증 방법]

### 중복 발생 위험 케이스 (Duplicate Fire Risk) 🔄
- 상황: [새로고침, 중첩 요소 등]
- 확인 방법: [검증 방법]

### 데이터 오류 위험 케이스 (Wrong Data Risk) 📊
- 상황: [잘못된 요소 데이터 수집 등]
- 확인 항목: [파라미터명과 예상값]
```

### 이벤트별 고려 사항

#### 클릭 이벤트 (select_promotion, select_item, ap_click 등)

| 위험 요소 | 설명 | 검증 방법 |
|----------|-----|----------|
| 이벤트 버블링 | 중첩 요소에서 상위로 이벤트 전파 | 중첩 영역 클릭 테스트 |
| 비주얼 vs DOM 불일치 | 클릭 가능해 보이지만 속성 없음 | DOM 속성 확인 |
| 클릭 영역 모호함 | 체크박스 vs 텍스트 영역 | 각 영역별 클릭 테스트 |
| 데이터 수집 오류 | 상위 요소 데이터 수집 | 파라미터 값 검증 |

#### 페이지 로드 이벤트 (view_item, view_item_list 등)

| 위험 요소 | 설명 | 검증 방법 |
|----------|-----|----------|
| SPA 라우트 누락 | 히스토리 변경 시 미발생 | SPA 내비게이션 테스트 |
| 새로고침 중복 | F5 시 중복 발생 | 새로고침 테스트 |
| 무한 스크롤 | 추가 로드 시 중복 items | 스크롤 후 items 배열 확인 |
| 빈 결과 미처리 | 결과 0건 시 미발생 | 빈 검색 결과 테스트 |

#### 트랜잭션 이벤트 (purchase)

| 위험 요소 | 설명 | 검증 방법 |
|----------|-----|----------|
| 중복 발생 | 새로고침 시 매출 중복 | 완료 페이지 새로고침 |
| 결제 실패 오발생 | 실패해도 이벤트 발생 | 결제 실패 시나리오 |
| 간편결제 누락 | 외부 앱 리다이렉트 후 누락 | 간편결제 플로우 테스트 |

**상세 내용**: `docs/event-validation-scenarios.md` 참조

---

## 체크리스트

가이드 파일 작성/수정 시 다음 항목을 확인하세요:

### 필수 확인
- [ ] 하드코딩된 dataLayer event명이 없는가?
- [ ] 하드코딩된 CSS Selector가 없는가?
- [ ] site-config.json 참조 안내가 있는가?
- [ ] "사이트별 예시" 테이블이 있는가?
- [ ] 트리거 타입이 명확히 구분되어 있는가?

### 트리거 타입별 확인
- [ ] CUSTOM_EVENT: dataLayer.push 구조 예시가 있는가?
- [ ] LINK_CLICK: DOM 속성 구조 예시가 있는가?
- [ ] 시나리오 작성 지침이 트리거 타입과 일치하는가?

### 일관성 확인
- [ ] 다른 가이드 파일과 형식이 일관되는가?
- [ ] eventMapping 키 이름이 site-config.json과 일치하는가?
- [ ] domAttributes 키 이름이 site-config.json과 일치하는가?

### 검증 시나리오 확인
- [ ] 오발생 위험 케이스가 포함되어 있는가? (이벤트 버블링 등)
- [ ] 중복 발생 위험 케이스가 포함되어 있는가? (새로고침 등)
- [ ] 데이터 오류 위험 케이스가 포함되어 있는가?
- [ ] 비주얼 vs DOM 불일치 상황이 설명되어 있는가?

---

## 참조 파일

| 파일 | 설명 |
|-----|------|
| `docs/multi-site-architecture.md` | 멀티사이트 아키텍처 설명 |
| `docs/gtm-parsing-guide.md` | GTM JSON 파싱 방법 |
| `docs/event-validation-scenarios.md` | 이벤트 검증 시나리오 상세 |
| `src/types/siteConfig.ts` | site-config 타입 정의 |
| `sites/amoremall-kr/site-config.json` | Amoremall 설정 예시 |
